{"name":"Unpack Dailies code","on":{"push":{"paths":["dailies-repo.zip"]}},"permissions":{"contents":"write"},"jobs":{"unpack":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"unzip -o dailies-repo.zip && git rm -q dailies-repo.zip && git config user.name github-actions && git config user.email github-actions@users.noreply.github.com && git add -A && git commit -m 'Add Dailies site code' && git push"}]}}}
