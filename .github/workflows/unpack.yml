{"name":"Unpack Dailies code","on":{"push":{"paths":["dailies.b64"]}},"permissions":{"contents":"write"},"jobs":{"unpack":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"base64 -d dailies.b64 > /tmp/d.tar.xz && tar -xJf /tmp/d.tar.xz && git rm -q dailies.b64 && git config user.name github-actions && git config user.email github-actions@users.noreply.github.com && git add -A && git commit -m 'Add Dailies site code' && git push"}]}}}
